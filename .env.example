# ログイントークン署名鍵(必須・長いランダム文字列)
JWT_SECRET=change-me-to-a-long-random-string
# ポート(任意)
PORT=3000
# DB保存先(任意・Renderの永続ディスクなら /data)
DATA_DIR=./storage
# AIチャット(任意・設定するとClaudeによる本格応答)
ANTHROPIC_API_KEY=
AI_MODEL=claude-sonnet-4-6
